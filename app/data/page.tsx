'use client';
import { useState } from 'react';import { AlertTriangle,CheckCircle2,Download,FileArchive,FileSpreadsheet,Upload } from 'lucide-react';import AppHeader from '@/components/AppHeader';
type Preview={components:number;locations:number;stock:number;images:number;errors:string[]};
export default function DataPage(){const [file,setFile]=useState<File|null>(null);const [preview,setPreview]=useState<Preview|null>(null);const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);
 async function send(commit:boolean){if(!file)return;setBusy(true);setMessage('');const fd=new FormData();fd.append('file',file);fd.append('commit',String(commit));const r=await fetch('/api/import',{method:'POST',body:fd});const data=await r.json() as Preview&{error?:string;imported?:{components:number;locations:number;stock:number;images:number}};setBusy(false);if(!r.ok){setMessage(data.error||'Import failed.');return;}if(commit&&data.imported){setMessage(`Import complete: ${data.imported.components} components, ${data.imported.locations} locations, and ${data.imported.stock} placements processed.`);setPreview(null);setFile(null);}else setPreview(data);}
 return <main className="app-shell"><AppHeader/><div className="page-wrap narrow"><div className="page-title-row"><div><p className="kicker">BACKUP & TRANSFER</p><h1>Your data</h1><p>Keep a portable copy of the full inventory or bring records in from CSV.</p></div></div><div className="data-grid"><section className="panel data-card"><span className="data-icon"><Download/></span><h2>Export a backup</h2><p>Download components, locations, stock placements, and uploaded images in one ZIP package.</p><ul><li><FileSpreadsheet size={16}/> Three readable CSV files</li><li><FileArchive size={16}/> Original component images</li></ul><a className="button primary" href="/api/export"><Download size={16}/> Download backup</a></section>
 <section className="panel data-card"><span className="data-icon orange"><Upload/></span><h2>Import inventory</h2><p>Restore a Smart Storage ZIP backup or add an individual components, locations, or stock CSV file.</p><label className="drop-zone"><input type="file" accept=".zip,.csv,application/zip,text/csv" onChange={e=>{setFile(e.target.files?.[0]||null);setPreview(null);setMessage('')}}/><Upload size={24}/><strong>{file?file.name:'Choose a ZIP or CSV file'}</strong><span>{file?`${(file.size/1024).toFixed(1)} KB`:'Maximum 25 MB'}</span></label><button className="button" disabled={!file||busy} onClick={()=>send(false)}>{busy?'Checking…':'Preview import'}</button></section></div>
 {preview&&<section className="panel import-preview"><div className="preview-head"><div><p className="kicker">IMPORT PREVIEW</p><h2>Review before importing</h2></div>{preview.errors.length?<AlertTriangle className="warning"/>:<CheckCircle2 className="okay"/>}</div><div className="preview-stats"><div><strong>{preview.components}</strong><span>components</span></div><div><strong>{preview.locations}</strong><span>locations</span></div><div><strong>{preview.stock}</strong><span>placements</span></div><div><strong>{preview.images}</strong><span>images</span></div></div>{preview.errors.length>0?<div className="error-list"><strong>Fix these errors:</strong>{preview.errors.map((e,i)=><p key={i}>{e}</p>)}</div>:<div className="notice success">The file is valid. Existing records with the same stable ID will be updated.</div>}<div className="form-actions"><button className="button primary" disabled={preview.errors.length>0||busy} onClick={()=>send(true)}><Upload size={16}/>{busy?'Importing…':'Import records'}</button></div></section>}{message&&<div className={message.startsWith('Import complete')?'notice success page-notice':'notice page-notice'}>{message}</div>}
 <section className="data-note"><AlertTriangle size={18}/><div><strong>Keep backups somewhere safe.</strong><p>Smart Storage stores its working database and photos in the local <code>data</code> folder. Exporting regularly protects against computer or disk failure.</p></div></section></div></main>}
